**Go/no-go — NO: slice 1 still requires number-changing decisions about inheritance, polling, filtered accounting and aggregation; the remaining gaps are not merely presentation.**

## §6 list closure

References are to the current documents in this directory. Verification was static: all three requested documents were read, record lists were compared mechanically, and relevant repository/Pi 0.85.1 contracts were inspected. No corpus analysis, execution sweep or implementation was performed.

1. **Root, filters and grouping — unanswered (partial).** Contracts → Commands (`spec.md:144–148`) now specifies root precedence as “the flag; `PI_CODING_AGENT_SESSION_DIR`; the agent directory's `sessions/` subdirectory”, discovery “recursively”, and `--since` on “the session header timestamp, inclusive, interpreted in UTC”. “Grouping is deliberately not a slice-1 flag.” Those choices are closed. `--model` “matches per-message model ids exactly” and metrics cover “only matching turns”, but the treatment of non-turn entries and unjoined results remains unspecified: compactions have no recorded model id, and orphan results have no issuing turn to match. The interaction with streaks also needs a rule: does an excluded model's successful result still break a failure streak?

2. **JSON shape, version and metric names — unanswered (partial).** Contracts → JSON shape (`spec.md:164–170`) supplies “A versioned object on stdout, never a stream” and `{ version, kind, root, sessions[], summary{}, diagnostics{}, suppressed_polling_repeats[] }`; Metric field names (`:174–178`) moves all five named record lists into the spec. Still absent: the version value/type; the `sessions[]` element shape and placement of `StruggleFacts`; the summary's fields and aggregation rules; field types and unavailable-value encoding. `repeat_occurrences` is required by `:182` but absent from the field list at `:176`; the usage buckets promised at `:189` are unnamed.

3. **Exit codes and incomplete scans — answered.** Contracts → Exit statuses (`spec.md:156–160`) gives 0 success, 1 assertions failed, 2 usage error, 3 “Partial scan — some files unreadable, totals exclude them”, and 4 infrastructure failure; JSON shape (`:164–170`) supplies the output envelope and skipped-file diagnostics.

4. **Polling names, command pattern and suppression records — unanswered (partial).** Contracts → Polling suppression (`spec.md:197–199`) enumerates “`terminal_wait`, `terminal_check`, `terminal_list`, `subagent_wait`, `subagent_check`, `subagent_list`”; JSON shape (`:170`) gives `{ session_id, tool, args_digest, count, pattern }`. But “contains a `sleep <digits>` invocation” is not an exact matcher: substring versus shell-invocation matching, quoting and numeric-token boundaries remain choices. For example, a textual matcher can suppress `echo 'sleep 300'`, and an integer-prefix matcher can match `sleep 0.5`. The pattern/version identifier and digest encoding are unspecified. “Every suppressed repeat” (`:170`) versus “every suppressed occurrence” (`:199`) also leaves whether singleton polling calls appear, row granularity, and whether `count` includes the first occurrence unresolved.

5. **Repeat/fail-repeat/streak/pressure definitions and canonicalisation — answered.** Contracts → Metric definitions (`spec.md:182–186`) defines repeat pairs as “distinct canonical keys” occurring “two or more times”, canonicalisation with “object keys sorted recursively”, failed pairs as keys with “two or more failed” occurrences, streaks as “consecutive joined results” in branch entry order, and pressure as “compactions per 100 assistant turns”, unavailable at zero turns; filter composition remains the separate gap in item 1.

6. **Accounted branch and cross-file forks — unanswered (partial).** Contracts → Branch rule (`spec.md:193`) fixes the branch as “the path from the session's root entry to its last entry in file order” and says inherited entries are “counted as `inherited_entries` and excluded from the derived metrics”. It does not define how inherited entries are identified or what happens when the referenced parent is outside the scan root, missing or unreadable. Pi records a parent path, not a per-entry inheritance marker; `forkFrom` copies non-header entries, while `createBranchedSession` can re-chain their parents (installed `dist/core/session-manager.js:1093–1166,1264–1304`). The exclusion policy is settled; its identification and unavailable-parent rules are not.

7. **Compaction/branch-summary and non-assistant usage — unanswered (partial).** Contracts → Metric definitions (`spec.md:189`) now says “Compaction and branch-summary usage and nested tool usage are counted in their own buckets rather than folded into turn totals.” Separation is answered, but bucket schemas, roll-up rules and partial-availability accounting are not. Repository types provide optional `CompactionEntry.usage`, `BranchSummaryEntry.usage` and `ToolResultMessage.usage`; they do not select the analytics policy. Likewise, Pi's `Usage.reasoning` is explicitly absent for providers without a breakdown: the spec does not say whether a mixture of reported and unreported reasoning produces an unavailable total or a labelled known subtotal.

8. **Terminal table and stderr diagnostics — answered.** Contracts → Commands (`spec.md:144`) specifies “one row per session with turns, calls, failure rate with its denominator, cost, and the struggle facts; a totals row; and a trailing line naming how many files were skipped”; JSON shape (`:170`) specifies “one `skip <path>: <reason>` line each”. Exact widths and rounding are presentation discretion, not missing accounting decisions.

9. **Launcher dispatch and script location — unanswered (presentation/implementation placement only).** Contracts → Commands (`spec.md:142`) specifies the existing `doctor`/`diag` command-`case` pattern, delegating “to a short script” without changing runtime selection. That closes coexistence with positional parsing. The script's actual path/name is still unstated. This is not a numerical blocker: the repository precedent is `scripts/diag-summary.mjs` (`bin/sumocode.sh:951`), and choosing another private implementation filename need not be frozen in the contract.

10. **Malformed-line diagnostic ownership — answered.** Implementation Decisions (`spec.md:98`) explicitly says “the analysis layer owns its own line parsing” and reports skips; US 19 (`:47`) requires “a skipped file with a reason”. Reusing pure tree construction is possible without the silent-drop entry reader: `src/sumo-tui/rpc/session-tree.ts:39` exports `buildSessionTreeFromEntries`.

## §4 regression status

1. **Not resolved completely — overflow ownership is corrected, location/lifetime are not.** `spec.md:106` now requires writing “everything it removed to a run-owned overflow file, appending that path”. The handler can do this: installed Pi `docs/extensions.md:111,152` permits full system permissions and Node filesystem imports, and `dist/core/extensions/runner.js:703` awaits the handler before applying its content patch. The spec still supplies neither a directory anchor/how the handler obtains it nor an explicit retention/cleanup boundary ensuring later reads work; “run-owned” and generic teardown ownership (`spec.md:115–119`) are not that protocol. This is not a slice-1 blocker.
2. **Resolved — field-list relocation.** Contracts → Metric field names (`spec.md:174–178`) now enumerates `SessionMetrics`, `StruggleFacts`, `HarnessVariant`, `EvalRun` and `Fixture`; remaining schema incompleteness is covered above, not an unchanged “only in the plan” finding.
3. **Resolved — comparator, types and weights landed.** `spec.md:203` states `shell`, `git`, `trace`, “positive numeric weight”, weighted mean and “greater than or equal to the threshold”; `plan.mdx:399–407` agrees on the comparator and type names (the trace predicate's input-domain mismatch is listed below).
4. **Resolved.** `spec.md:149`: below-minimum `--reps` “is raised to `min_reps` with a stderr diagnostic. There is no upper clamp.”
5. **Resolved.** `spec.md:131` and `plan.mdx:467` specify `session.html` and “Pi's own share-viewer helper applied to the returned gist id”.
6. **Resolved.** `spec.md:130` and `plan.mdx:467` require reporting raw `gh` output and “no retry” when exit 0 supplies no parseable URL.
7. **Not resolved across both documents.** Declaration (`spec.md:211`) explicitly says “not pinned to a revision in this work” and makes these the spec's own names, correctly eliminating the unnamed-pin obligation; `plan.mdx:128` still claims “pinned to a revision”.
8. **Not resolved completely.** `plan.mdx:278` now honestly calls it “a declared name list plus one declared argument pattern”, and `spec.md:197–199` supplies the names and sleep-shaped description, but the exact matcher and version remain unspecified (§6 item 4).
9. **Resolved.** `plan.mdx:22` now says “all three were wrong”, agreeing with `spec.md:241`'s three corrected premises.
10. **Resolved.** State directory (`spec.md:207`) now names the user's SumoCode state directory and `SUMOCODE_ANALYTICS_DIR`, with “Nothing is written into a checkout”; the existing state-root resolver is `src/activity/persistence.ts:187–191`.
11. **Resolved.** `plan.mdx:223` now says `quality (experience deferred)` and `:248` says `string (synthesized repo name, never a source path)`, consistent with the spec's deferral and synthesis decisions.
12. **Resolved.** `spec.md:98` explicitly assigns line parsing to the analysis layer instead of the reused reader; `:47` requires file-level skip diagnostics.
13. **Resolved.** The affected workspace story is now grammatical: “each run isolated in a run-owned workspace, so that…” (`spec.md:60`).

## Contradictions found

1. **Revision pin claim remains stale.** `plan.mdx:128` says “pinned to a revision”; `spec.md:211` says “not pinned to a revision in this work”. The Declaration is sufficient for the standalone spec; the plan's table needs to reflect it.

2. **Record/table shapes still drift.** The plan's `SessionMetrics` DataModel (`plan.mdx:183–205`) omits `inherited_entries` and `calls_without_result`, both required by `spec.md:174`; `plan.mdx:436` nevertheless promises “the record shapes above”. Additionally, `spec.md:182` requires reporting `repeat_occurrences`, absent from both record lists, and `spec.md:189` requires unnamed usage buckets absent from both. The plan's numeric-only `compaction_pressure` type (`:217`) also does not express the unavailable case explicitly required by `spec.md:186`. These are stale/incomplete shapes, not different spellings for shared fields: the mechanical comparison found no renamed common field.

3. **Unknown-entry skip scope remains stale in the plan.** `plan.mdx:519` says “unknown entry types are skipped rather than fatal”; `spec.md:47` requires an unknown entry to be reported as “a skipped file with a reason”. Entry-dropping while retaining the rest of the file would change totals and violate the current spec. The plan needs to say file exclusion, not leave the old entry-skipping description in place.

4. **Trace assertion input domain differs.** `spec.md:203` defines `trace` as “a predicate over struggle facts”, but `plan.mdx:407` includes a `trace` assertion for “recorded cost within the fixture's ceiling”. Cost belongs to `SessionMetrics`, not `StruggleFacts` (`spec.md:174–176`). This is a contract mismatch exposed by the requested assertion check, not a reason to delay slice 1 or redesign scoring now.

**Command/summary cross-check:** no conflicting command form, numeric exit-code assignment or threshold comparator was found. The plan's “one object per session plus a corpus summary” (`:436`) is compatible with nested `sessions[]`: its next paragraph explicitly says “A versioned object, not a stream”. Nor does that `--json` sentence require the default human table to show every record field; `spec.md:144` deliberately makes it a short view.

**Open Questions check:** the generic constraints prompt (`plan.mdx:550–553`) mentions output locations and privacy, which the spec already constrains (`spec.md:207,225–228`); it can solicit additional constraints but should not present those settled boundaries as undecided. The first-sweep model question (`plan.mdx:556–565`) is not answered by the spec and is outside slice 1. Conversely, the unresolved slice-1 accounting choices below are not surfaced in that block.

## Still missing for slice 1

1. **Changes numbers — inherited-entry identification and unavailable parents.** Define the identity test against the parent, whether parent lookup may leave `--root`, and the result when the source cannot be read. This determines which child entries contribute metrics or whether that session becomes unavailable/skipped. The decision to exclude identified inherited entries is already settled and should not be reopened (`spec.md:193`).

2. **Changes numbers — exact polling matcher and suppression counts.** Supply the executable pattern or equivalently precise matching grammar, including quote/token-boundary behaviour. Define whether suppression records cover singleton calls or only repeated keys, whether a row represents a key or an occurrence, and whether `count` includes the first call. These affect repeat facts and reported suppression counts (`spec.md:170,197–199`).

3. **Changes numbers — model-filter accounting.** Specify the source model field (`model` versus a `responseModel` override, both available in repository types), attribution/exclusion of compactions and unjoined results, and whether filtered-out successful results break streaks. For an A-failure, B-success, A-failure sequence, the last choice changes `error_streak_max` under `--model A` from 1 to 2 (`spec.md:148,185`).

4. **Changes numbers — remaining accounting and aggregation rules.** Define eligible tool outcomes explicitly in the spec: the plan alone says the denominator is joined results for that tool (`plan.mdx:175`), leaving orphan failures' participation unstated in the standalone contract. Specify the cache-read-share denominator promised by US 7 (`spec.md:35`). Define the auxiliary usage buckets' aggregation and mixed-availability policy, including unreported reasoning. Finally, enumerate summary measures and their reducers: `summary{}` does not decide, for example, whether repeat keys are counted per session or re-deduplicated across sessions, or how compaction pressure is combined (`spec.md:167,182–189`). These are accounting omissions, not a request to reconsider the settled statistics/reporting design.

5. **Presentation only — finish the machine shape and non-numeric conventions.** Pin the JSON/metric version value; `sessions[]` nesting; record and bucket field names/types, including `repeat_occurrences`; unavailable-value encoding; suppression pattern identifiers and argument-digest encoding; and whether repeat locations enumerate all occurrences or a representative entry. Define the diagnostic counters' scan/filter scope if they are intended as reproducible coverage counts. The unnamed dispatch script from §6 item 9 is also non-numeric implementation placement, not a blocker to correct metrics. Table widths need no further decision. **Counter scope is the exception within this schema work: it changes the reported diagnostic numbers**, even though it does not change session metrics.
